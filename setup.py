from setuptools import setup, find_packages

setup(name='group_amplification',
      version='0.1.0',
      description='group privacy amplification',
      packages=find_packages(),
      zip_safe=False)
